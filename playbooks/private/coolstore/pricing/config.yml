---
- name: CoolStore Pricing Checkpoint Start
  hosts: all
  gather_facts: false
  tasks:
  - name: Set Cluster Operator 'In Progress'
    run_once: true
    set_stats:
      data:
        installer_phase_coolstore_pricing_application:
          title: "CoolStore Gatewau"
          playbook: "playbooks/coolstore/pricing/config.yml"
          status: "In Progress"
          start: "{{ lookup('pipe', 'date +%Y%m%d%H%M%SZ') }}"


- name: Configure CoolStore Pricing
  hosts: all
  gather_facts: false
  vars:
    openshift_ansible_path: "/usr/share/ansible/openshift-ansible"
  tasks:
    - name: Install CoolStore Pricing
      include_role:
        name: coolstore
        tasks_from: pricing

- name: CoolStore Pricing Checkpoint End
  hosts: all
  gather_facts: false
  tasks:
  - name: Set Cluster Operator 'Complete'
    run_once: true
    set_stats:
      data:
        installer_phase_coolstore_pricing_application:
          status: "Complete"
          end: "{{ lookup('pipe', 'date +%Y%m%d%H%M%SZ') }}"
