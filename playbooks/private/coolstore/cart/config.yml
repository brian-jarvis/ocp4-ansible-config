---
- name: CoolStore Cart Checkpoint Start
  hosts: all
  gather_facts: false
  tasks:
  - name: Set Cluster Operator 'In Progress'
    run_once: true
    set_stats:
      data:
        installer_phase_coolstore_cart_application:
          title: "CoolStore Gatewau"
          playbook: "playbooks/coolstore/cart/config.yml"
          status: "In Progress"
          start: "{{ lookup('pipe', 'date +%Y%m%d%H%M%SZ') }}"


- name: Configure CoolStore Cart
  hosts: all
  gather_facts: false
  vars:
    openshift_ansible_path: "/usr/share/ansible/openshift-ansible"
  tasks:
    - name: Install CoolStore Cart
      include_role:
        name: coolstore
        tasks_from: cart

- name: CoolStore Cart Checkpoint End
  hosts: all
  gather_facts: false
  tasks:
  - name: Set Cluster Operator 'Complete'
    run_once: true
    set_stats:
      data:
        installer_phase_coolstore_cart_application:
          status: "Complete"
          end: "{{ lookup('pipe', 'date +%Y%m%d%H%M%SZ') }}"
